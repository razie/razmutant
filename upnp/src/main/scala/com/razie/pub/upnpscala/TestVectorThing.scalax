package com.razie.pub.upnpscala

class TestVectorS extends TestVectorJ {
   override def v = {
      val vec = super.v
      val arr = vec.toArray()
      for (i <- 0 to vec.size-1)
         vec.get(i)
      vec
   }
}