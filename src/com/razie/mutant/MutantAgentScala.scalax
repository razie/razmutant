package com.razie.mutant

import com.razie.pub.comms._
import com.razie.sdk.agent._
import com.razie.agent.network._

class MutantAgentScala (myHandle:AgentHandle, homeCloud:AgentCloud) 
extends MutantAgent(myHandle, homeCloud) {
   override def hackinit1 = {
		register(new AgentNetworkService());
   }
}
